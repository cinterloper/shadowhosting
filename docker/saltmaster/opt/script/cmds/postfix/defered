CTYPE( )
{
 echo application/json
}

STRATEGY( )
{
  echo async
}

RUN( )
{
 data=$(cd /var/spool/postfix/deferred/; find . -type f | cut -d '/' -f3- )
 $JQPath -n --arg data "$data" '{deferred: ($data|split("\n"))}'
 CALLBACK $CBDATA
}
