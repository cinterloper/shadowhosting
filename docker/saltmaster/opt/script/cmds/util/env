#!/bin/bash
ARGS='"ARGS":{"SSHPORT":"int", "TESTVAR":"String"}'     #this decleration MUST be on the 2nd line, and in this format
CTYPE( )
{
 echo application/json
}

STRATEGY( )
{
  echo async
}

RUN( )
{
 output=$(env)
 $JQPath -n --arg output "$output" '{env: $output}'
 CALLBACK $CBDATA
}
